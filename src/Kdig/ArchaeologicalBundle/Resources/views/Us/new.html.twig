{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Us creation</h1>

    <form action="{{ path('us_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('us') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
