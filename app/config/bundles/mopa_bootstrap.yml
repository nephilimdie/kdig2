mopa_bootstrap:
    form:
        show_legend: false # default is true
        show_child_legend: false # default is true
        error_type: inline 
        checkbox_label: 'widget' # default is both (label|widget|both)
        collection:
            widget_remove_btn:
                icon: trash
                icon_color: white
            widget_add_btn:
                icon: plus-sign
    navbar:
        template: KdigTemplateBundle:Default:Navbar/navbar.html.twig
